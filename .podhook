echo hook works
npm install
./node_modules/.bin/webpack --config=webpack.config.prod.babel.js
passed=$?
if [[ $passed != 0 ]]; then
    # test failed, exit. app's working tree on the server will be reset.
    exit $passed
fi
