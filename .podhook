echo hook works
npm install
npm run build
./node_modules/.bin/babel server --out-dir server:es5 --copy-files
passed=$?
if [[ $passed != 0 ]]; then
    # test failed, exit. app's working tree on the server will be reset.
    exit $passed
fi
